package io.github.jasper.monitoring.api.code;

/** Governance family for a stable machine-readable code. */
public enum CodeFamily {
    OUTCOME_REASON,
    INPUT_DIAGNOSTIC,
    OPERATIONAL_FAILURE,
    API_ERROR
}
