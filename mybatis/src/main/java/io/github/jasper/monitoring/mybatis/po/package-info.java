/**
 * Persistence objects used exclusively by MyBatis mappers and repository adapters.
 *
 * <p>These types mirror database rows and are intentionally distinct from immutable core domain
 * objects. They must not leak into the framework-neutral API module.</p>
 */
package io.github.jasper.monitoring.mybatis.po;
